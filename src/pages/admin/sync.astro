---
/**
 * Senkronizasyon askıya alındı (bkz. src/lib/pms/api.ts → SYNC_ENABLED).
 * Rezervasyonlar artık yalnızca elle giriliyor. Bu rota src/middleware.ts
 * tarafından /admin'e yönlendirildiği için normalde hiç render edilmez;
 * bu dosya yalnızca güvenlik ağı olarak (ör. middleware devre dışı bırakılırsa)
 * geçerli bir sayfa olarak kalır.
 */
import AdminLayout from "@layouts/AdminLayout.astro";

export const prerender = false;
---

<AdminLayout title="Senkronizasyon">
  <p class="rounded-xl border border-stone-200 bg-white p-6 text-sm text-stone-500">
    Senkronizasyon şu an devre dışı. <a href="/admin" class="underline">Takvime dön</a>.
  </p>
</AdminLayout>
